/**
 * ProductType interface for the product type
 *
 * @export
 * @interface ProductType
 */
export interface ProductType {
  title: string;
  price: number;
  description: string;
  image: string;
}
